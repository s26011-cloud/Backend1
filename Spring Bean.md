# Spring Bean이란
- Spring IoC(Inversion or Control) 컨테이너가 관리하는 자바 객체를 의미한다.

## 주요 특징
- IoC: 객체의 생성, 생명주기 관리, 의존성 설정(DI)을 개발자가 직접하지 않고 프레임워크가 대신 담당한다.

- POJO: 특정 클래스를 상속받거나 복잡한 인터페이스를 구현할 필요 없이, 평범한 자바 객체 형태로 만들 수 있어 유연성이 높다.

## 전체 흐름
- 스프링 컨테이너 생성

- 스프링 빈 생성

- 의존관계 주입

- 초기화 콜백

-  빈 사용

-  소멸 전 콜백

- 스프링 종료